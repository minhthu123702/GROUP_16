src/main/java/com/example/hanoiapp/
├── config            # Cấu hình Spring, Security, JWT, Bean chung
├── controller        # REST API Controller (xử lý request HTTP)
├── dto               # Data Transfer Object (các lớp request/response)
├── entity            # Entity JPA (mô hình database)
├── exception         # Xử lý lỗi custom (Exception Handling)
├── repository        # JpaRepository cho truy vấn database
├── security          # Các lớp liên quan Spring Security và JWT
├── service           # Service xử lý nghiệp vụ chính
└── util              # Các tiện ích chung (ví dụ mã hóa, validate, ...)
